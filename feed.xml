<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://yehya-farhat.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://yehya-farhat.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2025-11-07T21:43:35+00:00</updated><id>https://yehya-farhat.github.io/feed.xml</id><title type="html">blank</title><subtitle>Personal Website; Yehya Farhat </subtitle><entry><title type="html">How to Build Your Own Custom Optimizer in PyTorch (Step-by-Step)</title><link href="https://yehya-farhat.github.io/blog/2025/how-to-build-your-own-custom-optimizer-in-pytorch-step-by-step/" rel="alternate" type="text/html" title="How to Build Your Own Custom Optimizer in PyTorch (Step-by-Step)"/><published>2025-06-30T16:58:41+00:00</published><updated>2025-06-30T16:58:41+00:00</updated><id>https://yehya-farhat.github.io/blog/2025/how-to-build-your-own-custom-optimizer-in-pytorch-step-by-step</id><content type="html" xml:base="https://yehya-farhat.github.io/blog/2025/how-to-build-your-own-custom-optimizer-in-pytorch-step-by-step/"><![CDATA[<p>PyTorch comes with a rich set of built-in optimizers like SGD, Adam, and RMSProp — more than enough for most deep learning tasks. But what if you’re reading a cutting-edge paper with a brand-new optimizer? Or maybe you’ve come up with your own twist on an existing algorithm and want to see how it performs. That’s where writing your own optimizer comes in. In this hands-on tutorial, we’ll demystify the process by walking through how to create a custom optimizer in PyTorch. As a concrete example, we’ll reimplement the Adam optimizer — step by step — so you can understand exactly how everything fits together under the hood.</p> <p>Before we dive in, I’ll assume you have a basic understanding of gradient-based optimization, basic PyTorch structure, and how Python classes work. While brief refreshers are included for context, this tutorial builds on those core concepts. Our focus will be on machine learning models, but the techniques apply to optimizing any differentiable function.</p> <h3>1st order optimization</h3> <p>First order optimization is a category of algorithms that is used to find some set of unknown parameters. Usually in most cases you are trying to find a set of parameters that minimize some sort of error metric function (loss function). These algorithms rely on the gradient of these functions at certain points to estimate a descent direction. This process done iteratively until a minima is hit, meaning we have found our parameters (more or less). The classical known algorithm that does this is <em>Gradient Descent (GD).</em></p> <p>GD is defined by the following update rule</p> <figure><img alt="" src="https://cdn-images-1.medium.com/max/241/0*18SDW-ML1gJJHzfW.png"/></figure> <p><em>θ </em>here would be our unknown parameters, <em>μ</em> is our stepsize and <em>∇f(θ)</em> is the gradient of our loss function with respect to the parameters. Let us now ground this equation in the PyTorch training loop.</p> <h3>Standard Pytorch training loop</h3> <p>The following snippet of code is practically the most important section of your optimization procedure, it is where all the magic happens. I have labeled all the main steps of the process from 0–5.</p> <pre>....<br /># Loss Function<br />criterion = nn.CrossEntropyLoss()<br /><br /># --- Initialize Optimizer ---:<br /># 0. Init. the optimizer with model parameters and the learning rate as input<br />optimizer = torch.optim.SGD(model.parameters(), lr=learning_rate)<br /><br />for epoch in range(num_epochs):<br />    # Loop over the training dataset for one epoch<br />    for i, (images, labels) in enumerate(train_loader):<br />        images = images.reshape(-1, input_size).to(device)<br />        labels = labels.to(device)<br />        <br />        # --- Forward Pass ---<br />        # 1. Calculate the loss<br />        outputs = model(images)<br />        loss = criterion(outputs, labels)<br />        <br />        # --- Backward Pass ---<br />        # 2. Zero out the gradients from the previous step<br />        optimizer.zero_grad()<br />        # 3. Perform backpropagation to compute gradients<br />        loss.backward()<br />        # 4. Update the model&#39;s weights<br />        optimizer.step()<br />....</pre> <ul><li><strong>Step 0</strong>: Initialize optimizer = torch.optim.SGD(model.parameters(), lr = learning_rate). Each optimizer initialization internally calls the __init__() function, invoking the initialization of the class instance and passing in the model parameters as input along with the stepsize. Keep this __init__()<strong> </strong>function in mind as when we want to build our own custom optimizer we will need to invoke our own __init__() method.</li></ul> <p>Following this we then have our standard training loop.</p> <ul><li><strong>Step 1</strong>: We compute the forward pass, in other words we compute our loss function <strong><em>f(θ).</em></strong></li><li><strong>Step 2: </strong>PyTorch requires us to manually zero gradients optimizer.zero_grad()before calling backward, as gradients accumulate by default to allow flexibility in more complex optimization strategies.</li><li><strong>Step 3</strong>:<em> </em>loss.backward() will calculate the gradient of our loss function <strong><em>∇f(θ) </em></strong>(with respect to the parameters passed in on initialization).</li><li><strong>Step 4:</strong> optimizer.step()will update the the model parameters using the gradient calculated in Step 3. In other words this performs the following operation <strong><em>θ — μ∇ f(θ).</em></strong></li></ul> <p>With all of this in mind, building a custom optimizer essentially boils down to editing just two key parts: the __init__() function and the .step() function. By overriding these two methods, you can introduce virtually any functionality you want.</p> <p>Now, the fun part — and the main point of this post — is how exactly we go about editing these functions. PyTorch simplifies this task greatly: we simply inherit from the base class torch.optim.Optimizer and then override its __init__() and .step() methods. As promised I will take you through the steps of overriding both these functions to show you how we can implement our custom Adam optimizer.</p> <p>Before diving into the implementation details, let’s quickly define the Adam update step.</p> <h3>The Adam optimizer</h3> <figure><img alt="" src="https://cdn-images-1.medium.com/max/374/0*A2szmxzcCUTDtBgN.png"/></figure> <p>Here things are a bit more complicated. We need to calculate the first and second moment estimates of the gradients, mₜ₊₁ and vₜ₊₁, respectively. After which we need to compute the biased correct terms of our given estimates, m̂ₜ₊₁ and v̂ₜ₊₁. And finally peform our update step θₜ - (μ m̂ₜ₊₁/v̂ₜ₊₁ + ε).</p> <p>Let’s begin by defining our custom optimizer class, CustomAdam. To do this, we&#39;ll inherit from PyTorch&#39;s standard torch.optim.Optimizer class and override its __init__() method.</p> <h3>__init__()</h3> <pre>class CustomAdam(torch.optim.Optimizer):<br />    &quot;&quot;&quot;<br />    Custom Adam class <br />    &quot;&quot;&quot;<br />    # Define standard Hyperparameters <br />    def __init__(self, params, lr=0.01, beta1 =0.9, beta2 = 0.999, eps=1e-8, weight_decay=0):<br />         <br />        # invalid input errors<br />        if lr&lt;= 0.0:<br />            raise ValueError(f&quot;Invalid learning rate: {lr}&quot;)<br />        if eps&lt;= 0.0:<br />            raise ValueError(f&quot;eps needs to be a positive value&quot;)<br />        <br />        # define a dictionary for hyperparameters<br />        defaults = dict(lr=lr, beta1=beta1, beta2=beta2 , eps=eps, weight_decay=weight_decay)<br />        super(CustomAdam, self).__init__(params, defaults)<br />...</pre> <p>Note that the __init__() method is what python calls internally once you initialize any class. But notice, in our case __init__() is not only used to initialize our optimizer class instance but also defines its core functionality. For instance, it allows us to specify the optimizer&#39;s hyperparameters (e.g., lr, betas, eps, weight_decay) and set default values if no parameters are explicitly provided. Additionally, we can handle ill-conditioned input values here, such as non-positive or zero-valued learning rates or epsilons.</p> <p>Inside __init__(), we typically define a dictionary called defaults. This dictionary conveniently stores our hyperparameters, making them easy to access later in the .step() function or other internal methods we might create. PyTorch uses this defaults dictionary internally for managing param_groups (which we&#39;ll discuss shortly).</p> <p>With this foundation laid, we can now define the .step() method, which will handle the main computational logic.</p> <h3><strong>step()</strong></h3> <p>Recall that the .step() method is invoked at the end of each training iteration, immediately following the .backward() call. This means that at this stage, all parameters have their corresponding gradients computed and available.</p> <p>For gradient descent (GD), the .step() method would typically loop through each parameter, updating it by subtracting the product of the step size and the gradient from its current value. Let&#39;s briefly illustrate how this update looks in practice <strong>for</strong> <strong>standard GD</strong>.</p> <pre>...<br />@torch.no_grad()<br />def step(self):<br />    &quot;&quot;&quot;<br />    Performs a single optimization step for GD.<br />    &quot;&quot;&quot;<br />    # Iterate through each group<br />    for group in self.param_groups:<br />        lr = group[&#39;lr&#39;]<br />        # Iterate through each individual parameter in the group.<br />        for p in group[&#39;params&#39;]:<br />            if p.grad is None:<br />                continue<br />            # GD update rule:<br />            # new_weight = old_weight - learning_rate * gradient<br />            p.add_(-lr*p.grad)</pre> <p>PyTorch organizes parameters into individual <strong>“groups”. </strong>For all intents and purposes, all you need to know is that a “<strong>group”</strong> is a Python dictionary that bundles together a set of model parameters with their specific hyperparameters (e.g., learning rate) using defaults. For example, your normal base model parameters and your bias parameters will each reside in their own respective <strong>“group”</strong>, each potentially using distinct hyperparameters. This grouping allows great flexibility, as you can specify different hyperparameter values for each group by setting them individually in the defaults dictionary (we will not do this here).</p> <p>The collection of all these groups is self.param_groups which is a list of these dictionaries. Since, in this tutorial, we’re using identical hyperparameters across all groups, group[&#39;lr&#39;] remains consistent.</p> <p>To loop through and update each model parameter, we first need to iterate through its collection of parameter groups self.param_groups. After which we can access the individual parameters of that group, via group[&#39;params&#39;]. To perform the actual updates we loop through group[&#39;params&#39;] and apply the updates directly using the .add_() function to each individual parameter. This function efficiently modifies parameters in-place, ensuring fast updates without additional memory overhead.</p> <p>It’s worth noting that both parameter grouping and the defaults dictionary are PyTorch internals. While the grouping mechanism provides substantial flexibility, it’s often sufficient to use the same hyperparameters across all parameters, in which case the defaults dictionary might seem unnecessary. You technically could save hyperparameters directly as instance variables instead (e.g., self.lr = lr) and avoid using defaults. However, we’ll stick to PyTorch’s recommended structure in this tutorial for clarity and consistency.</p> <h4><strong>Adam step()</strong></h4> <p>With this we can now define the .step() method for Adam</p> <pre>    ...<br />    @torch.no_grad()<br />    def step(self):<br />        &quot;&quot;&quot;Performs a single adam optimization step.&quot;&quot;&quot;<br /><br />        for group in self.param_groups:<br />            lr = group[&#39;lr&#39;]<br />            beta1 = group[&#39;beta1&#39;]  <br />            beta2 = group[&#39;beta2&#39;]<br />            eps = group[&#39;eps&#39;]<br />            weight_decay = group[&#39;weight_decay&#39;]<br /><br />            for p in group[&#39;params&#39;]:<br />                if p.grad is None:<br />                    continue<br /><br />                state = self.state[p]<br /><br />                # State Initialization to save parameter information over iterations<br />                if len(state) == 0:<br />                    state[&#39;step&#39;] = 0<br />                    # m_t: Biased first moment estimate (like momentum)<br />                    state[&#39;exp_avg&#39;] = torch.zeros_like(p, memory_format=torch.preserve_format)<br />                    # v_t: Biased second raw moment estimate (adaptive learning rate)<br />                    state[&#39;exp_avg_sq&#39;] = torch.zeros_like(p, memory_format=torch.preserve_format)<br /><br />                # Get state variables for the current parameter<br />                exp_avg, exp_avg_sq = state[&#39;exp_avg&#39;], state[&#39;exp_avg_sq&#39;]<br />                <br />                # Increment step counter t<br />                state[&#39;step&#39;] += 1<br />                t = state[&#39;step&#39;]<br /><br />               <br />                if weight_decay != 0:<br />                    p.mul_(1 - lr * weight_decay)<br /><br />                # --- Core Adam Logic ---<br /><br />                # 1. Update the biased estimates for m_t and v_t<br />                exp_avg.mul_(beta1).add_(p.grad, alpha=1 - beta1)<br />                exp_avg_sq.mul_(beta2).addcmul_(p.grad, p.grad, value=1 - beta2)<br /><br />                # 2. Calculate the NUMERATOR: The bias-corrected first moment (m_hat_t)<br />                bias_correction1 = 1 - beta1 ** t<br />                numerator = exp_avg / bias_correction1<br />                <br />                # 3. Calculate the DENOMINATOR: The bias-corrected second moment (sqrt(v_hat_t)) + eps<br />                bias_correction2 = 1 - beta2 ** t<br />                # First, get v_hat_t<br />                v_hat_t = exp_avg_sq / bias_correction2<br />                # Then, calculate the full denominator<br />                denominator = v_hat_t.sqrt().add(eps)<br /><br />                # 4. Calculate the final update amount<br />                update_step = numerator / denominator<br /><br />                # 5. Apply the final update to the parameter<br />                # new_weight = old_weight - lr * (numerator / denominator)<br />                p.add_(-lr*update_step)</pre> <p>Adam requires maintaining both the first moment (mean of gradients) and second moment (uncentered variance of gradients) estimates for each parameter in order to compute adaptive update steps. In PyTorch, this is managed through an internal state dictionary that is automatically created for each parameter the first time it is seen by the optimizer. This dictionary allows us to store persistent, per-parameter information across iterations.</p> <p>Specifically, for Adam, we typically initialize and update three entries in the state dictionary for each parameter: &#39;step&#39;: the iteration counter, &#39;exp_avg&#39;: the exponential moving average of past gradients (first moment), &#39;exp_avg_sq&#39;: the exponential moving average of squared gradients (second moment). These values are continuously updated during training to compute the bias-corrected parameter updates.</p> <p>With this setup, everything becomes pretty straightforward. All that’s left is to compute the Adam update step and apply it to our parameters. These computations are neatly handled in steps 1–5 within the <strong># – core Adam logic section – </strong>.</p> <p>One thing to quickly note is how the calculations are performed. We use functions like .add_(), .mul_(), and .addcmul_(), which may look unusual at first. These are in-place operations, meaning they modify the tensor directly instead of creating a new one. This approach is more memory- and compute-efficient compared to writing expressions like exp_avg = beta1 * exp_avg + (1 - beta1) * p.grad, which create new tensors at each step. But you can use either.</p> <p>And that’s it! With this, we’ve successfully implemented our own custom Adam optimizer.</p> <h3><strong>Putting everything together</strong></h3> <pre>class CustomAdam(torch.optim.Optimizer):<br />    &quot;&quot;&quot;<br />    Custom Adam class <br />    &quot;&quot;&quot;<br />    # Define standard Hyperparameters <br />    def __init__(self, params, lr=0.01, beta1 =0.9, beta2 = 0.999, eps=1e-8, weight_decay=0):<br />         <br />        # invalid input errors<br />        if lr&lt;= 0.0:<br />            raise ValueError(f&quot;Invalid learning rate: {lr}&quot;)<br />        if eps&lt;= 0.0:<br />            raise ValueError(f&quot;eps needs to be a positive value&quot;)<br />        <br />        # define a dictionary for hyperparameters<br />        defaults = dict(lr=lr, beta1=beta1, beta2=beta2 , eps=eps, weight_decay=weight_decay)<br />        super(CustomAdam, self).__init__(params, defaults)<br /><br />    @torch.no_grad()<br />    def step(self):<br />        &quot;&quot;&quot;Performs a single adam optimization step.&quot;&quot;&quot;<br /><br />        for group in self.param_groups:<br />            lr = group[&#39;lr&#39;]<br />            beta1 = group[&#39;beta1&#39;]  <br />            beta2 = group[&#39;beta2&#39;]<br />            eps = group[&#39;eps&#39;]<br />            weight_decay = group[&#39;weight_decay&#39;]<br /><br />            for p in group[&#39;params&#39;]:<br />                if p.grad is None:<br />                    continue<br /><br />                state = self.state[p]<br /><br />                # State Initialization to save parameter information over iterations<br />                if len(state) == 0:<br />                    state[&#39;step&#39;] = 0<br />                    # m_t: Biased first moment estimate (like momentum)<br />                    state[&#39;exp_avg&#39;] = torch.zeros_like(p, memory_format=torch.preserve_format)<br />                    # v_t: Biased second raw moment estimate (adaptive learning rate)<br />                    state[&#39;exp_avg_sq&#39;] = torch.zeros_like(p, memory_format=torch.preserve_format)<br /><br />                # Get state variables for the current parameter<br />                exp_avg, exp_avg_sq = state[&#39;exp_avg&#39;], state[&#39;exp_avg_sq&#39;]<br />                <br />                # Increment step counter t<br />                state[&#39;step&#39;] += 1<br />                t = state[&#39;step&#39;]<br /><br />               <br />                if weight_decay != 0:<br />                    p.mul_(1 - lr * weight_decay)<br /><br />                # --- Core Adam Logic ---<br /><br />                # 1. Update the biased estimates for m_t and v_t<br />                exp_avg.mul_(beta1).add_(p.grad, alpha=1 - beta1)<br />                exp_avg_sq.mul_(beta2).addcmul_(p.grad, p.grad, value=1 - beta2)<br /><br />                # 2. Calculate the NUMERATOR: The bias-corrected first moment (m_hat_t)<br />                bias_correction1 = 1 - beta1 ** t<br />                numerator = exp_avg / bias_correction1<br />                <br />                # 3. Calculate the DENOMINATOR: The bias-corrected second moment (sqrt(v_hat_t)) + eps<br />                bias_correction2 = 1 - beta2 ** t<br />                # First, get v_hat_t<br />                v_hat_t = exp_avg_sq / bias_correction2<br />                # Then, calculate the full denominator<br />                denominator = v_hat_t.sqrt().add(eps)<br /><br />                # 4. Calculate the final update amount<br />                update_step = numerator / denominator<br /><br />                # 5. Apply the final update to the parameter<br />                # new_weight = old_weight - lr * (numerator / denominator)<br />                p.add_(-lr*update_step)</pre> <p>Before wrapping up, it’s worth noting that what I’ve shown here isn’t the only way to implement a custom optimizer, but it is PyTorch’s recommended structure. For instance, there are multiple ways to access model parameters; you don’t necessarily have to use group[&#39;params&#39;]. Similarly, you’re not required to use the defaults dictionary, you can store hyperparameters directly as instance variables if you prefer.</p> <p>The state dictionary is also optional. If you&#39;d rather not use it, you can manage the first and second moment estimates with your own custom lists or dictionaries, initialized in the __init__() method. Additionally, you&#39;re free to modularize the logic however you like. For example, instead of placing all the Adam update steps directly inside .step(), you could define a separate function like AdamUpdate() and call it from within .step().</p> <p>The beauty of PyTorch is the flexibility it offers. But with that flexibility comes a lot of responsibility. Inefficient implementations can quickly become performance bottlenecks during training. I have inadvertently turned training runs that should have taken minutes into hours. So while it’s great to experiment, always keep an eye on what you’re actually doing.</p> <h4>Minimal working example</h4> <p>Let’s now put our customAdam optimizer to the test by using it to train a small model and comparing its performance with PyTorch’s built-in Adam optimizer. If we did everything correctly, then theoretically performance should be exactly equal.</p> <pre>import torch<br />import torch.nn as nn<br />import torch.optim as optim<br />import torchvision<br />import torchvision.transforms as transforms<br />import matplotlib.pyplot as plt<br />import time<br />import copy<br />import random<br />import numpy as np<br /><br />class CustomAdam(torch.optim.Optimizer):<br />    &quot;&quot;&quot;<br />    Custom Adam class <br />    &quot;&quot;&quot;<br />    # Define standard Hyperparameters <br />    def __init__(self, params, lr=0.01, beta1 =0.9, beta2 = 0.999, eps=1e-8, weight_decay=0):<br />         <br />        # invalid input errors<br />        if lr&lt;= 0.0:<br />            raise ValueError(f&quot;Invalid learning rate: {lr}&quot;)<br />        if eps&lt;= 0.0:<br />            raise ValueError(f&quot;eps needs to be a positive value&quot;)<br />        <br />        # define a dictionary for hyperparameters<br />        defaults = dict(lr=lr, beta1=beta1, beta2=beta2 , eps=eps, weight_decay=weight_decay)<br />        super(CustomAdam, self).__init__(params, defaults)<br /><br />    @torch.no_grad()<br />    def step(self):<br />        &quot;&quot;&quot;Performs a single adam optimization step.&quot;&quot;&quot;<br /><br />        for group in self.param_groups:<br />            lr = group[&#39;lr&#39;]<br />            beta1 = group[&#39;beta1&#39;]  <br />            beta2 = group[&#39;beta2&#39;]<br />            eps = group[&#39;eps&#39;]<br />            weight_decay = group[&#39;weight_decay&#39;]<br /><br />            for p in group[&#39;params&#39;]:<br />                if p.grad is None:<br />                    continue<br /><br />                state = self.state[p]<br /><br />                # State Initialization to save parameter information over iterations<br />                if len(state) == 0:<br />                    state[&#39;step&#39;] = 0<br />                    # m_t: Biased first moment estimate (like momentum)<br />                    state[&#39;exp_avg&#39;] = torch.zeros_like(p, memory_format=torch.preserve_format)<br />                    # v_t: Biased second raw moment estimate (adaptive learning rate)<br />                    state[&#39;exp_avg_sq&#39;] = torch.zeros_like(p, memory_format=torch.preserve_format)<br /><br />                # Get state variables for the current parameter<br />                exp_avg, exp_avg_sq = state[&#39;exp_avg&#39;], state[&#39;exp_avg_sq&#39;]<br />                <br />                # Increment step counter t<br />                state[&#39;step&#39;] += 1<br />                t = state[&#39;step&#39;]<br /><br />               <br />                if weight_decay != 0:<br />                    p.mul_(1 - lr * weight_decay)<br /><br />                # --- Core Adam Logic ---<br /><br />                # 1. Update the biased estimates for m_t and v_t<br />                exp_avg.mul_(beta1).add_(p.grad, alpha=1 - beta1)<br />                exp_avg_sq.mul_(beta2).addcmul_(p.grad, p.grad, value=1 - beta2)<br /><br />                # 2. Calculate the NUMERATOR: The bias-corrected first moment (m_hat_t)<br />                bias_correction1 = 1 - beta1 ** t<br />                numerator = exp_avg / bias_correction1<br />                <br />                # 3. Calculate the DENOMINATOR: The bias-corrected second moment (sqrt(v_hat_t)) + eps<br />                bias_correction2 = 1 - beta2 ** t<br />                # First, get v_hat_t<br />                v_hat_t = exp_avg_sq / bias_correction2<br />                # Then, calculate the full denominator<br />                denominator = v_hat_t.sqrt().add(eps)<br /><br />                # 4. Calculate the final update amount<br />                update_step = numerator / denominator<br /><br />                # 5. Apply the final update to the parameter<br />                # new_weight = old_weight - lr * (numerator / denominator)<br />                p.add_(-lr*update_step)<br /><br /><br />device = torch.device(&quot;cuda:0&quot; if torch.cuda.is_available() else &quot;cpu&quot;)<br />print(f&quot;Using device: {device}&quot;)<br /><br /># Set a seed so both models start with the same random weights<br /># --- Ensure Full Reproducibility ---<br />torch.manual_seed(42)<br />random.seed(42)<br />np.random.seed(42)<br /><br /># for GPU reproducibility<br />torch.backends.cudnn.deterministic = True<br />torch.backends.cudnn.benchmark = False<br /><br /><br />transform = transforms.Compose(<br />    [transforms.ToTensor(),<br />     transforms.Normalize((0.5, 0.5, 0.5), (0.5, 0.5, 0.5))])<br /><br />trainset = torchvision.datasets.CIFAR10(root=&#39;./data&#39;, train=True,<br />                                        download=True, transform=transform)<br /><br />subset_indices = list(range(int(len(trainset)))) <br />trainset_subset = torch.utils.data.Subset(trainset, subset_indices)<br />trainloader = torch.utils.data.DataLoader(trainset_subset, batch_size=64,<br />                                          shuffle=False, num_workers=2)<br /><br /># --- Model ---<br />class SimpleCNN(nn.Module):<br />    def __init__(self):<br />        super(SimpleCNN, self).__init__()<br />        self.conv1 = nn.Conv2d(3, 16, 5)<br />        self.pool = nn.MaxPool2d(2, 2)<br />        self.conv2 = nn.Conv2d(16, 32, 5)<br />        self.fc1 = nn.Linear(32 * 5 * 5, 120)<br />        self.fc2 = nn.Linear(120, 84)<br />        self.fc3 = nn.Linear(84, 10)<br /><br />    def forward(self, x):<br />        x = self.pool(torch.relu(self.conv1(x)))<br />        x = self.pool(torch.relu(self.conv2(x)))<br />        x = x.view(-1, 32 * 5 * 5)<br />        x = torch.relu(self.fc1(x))<br />        x = torch.relu(self.fc2(x))<br />        x = self.fc3(x)<br />        return x<br /><br /># --- Create two identical models ---<br />model_custom = SimpleCNN().to(device)<br />model_pytorch = copy.deepcopy(model_custom).to(device)<br /><br /><br />LEARNING_RATE = 0.001<br />EPOCHS = 10<br />criterion = nn.CrossEntropyLoss()<br /><br /># --- Instantiate Optimizers ---<br />optimizer_custom = CustomAdam(model_custom.parameters(), lr=LEARNING_RATE)<br />optimizer_pytorch = optim.Adam(model_pytorch.parameters(), lr=LEARNING_RATE)<br /><br /><br />def train_model(model, optimizer, model_name):<br />    &quot;&quot;&quot;A helper function to train a given model.&quot;&quot;&quot;<br />    print(f&quot;--- Training {model_name} ---&quot;)<br />    start_time = time.time()<br />    losses = []<br />    model.train() # Set model to training mode<br />    for epoch in range(EPOCHS):<br />        running_loss = 0.0<br />        for inputs, labels in trainloader:<br />            inputs, labels = inputs.to(device), labels.to(device)<br />            optimizer.zero_grad()<br />            outputs = model(inputs)<br />            loss = criterion(outputs, labels)<br />            loss.backward()<br />            optimizer.step()<br />            running_loss += loss.item()<br />        <br />        avg_loss = running_loss / len(trainloader)<br />        losses.append(avg_loss)<br />        print(f&#39;Epoch [{epoch + 1}/{EPOCHS}], Loss: {avg_loss:.4f}&#39;)<br />    <br />    end_time = time.time()<br />    print(f&#39;Finished training {model_name}. Total time: {end_time - start_time:.2f}s\n&#39;)<br />    return losses<br /><br /><br />custom_adam_losses = train_model(model_custom, optimizer_custom, &quot;Custom Adam&quot;)<br />pytorch_adam_losses = train_model(model_pytorch, optimizer_pytorch, &quot;PyTorch Adam&quot;)<br /><br /><br />plt.figure(figsize=(10, 6))<br />plt.plot(range(1, EPOCHS + 1), custom_adam_losses, marker=&#39;o&#39;, linestyle=&#39;-&#39;, label=&#39;Custom Adam&#39;)<br />plt.plot(range(1, EPOCHS + 1), pytorch_adam_losses, marker=&#39;x&#39;, linestyle=&#39;--&#39;, label=&#39;PyTorch optim.Adam&#39;)<br />plt.title(&#39;Optimizer Comparison: Training Loss on CIFAR-10&#39;)<br />plt.xlabel(&#39;Epoch&#39;)<br />plt.ylabel(&#39;Average Loss&#39;)<br />plt.legend()<br />plt.grid(True)<br />plt.xticks(range(1, EPOCHS + 1))<br /></pre> <figure><img alt="" src="https://cdn-images-1.medium.com/max/846/1*27nVwrtnT-TloT5571NIPw.png"/></figure> <p>Comparing the training loss of our custom optimizer with PyTorch’s implementation shows that they are effectively identical. However, keep in mind that exact reproducibility isn’t always guaranteed, as deep learning pipelines, particularly those involving GPUs, often introduce variability.</p> <h4>Summary</h4> <p>In this tutorial, we walked through a step-by-step guide for building custom optimizers in PyTorch. PyTorch makes this process straightforward by allowing you to inherit from the torch.optim.Optimizer class and override its two key methods: __init__() and step(). By customizing these methods—and using features like parameter groups and the state dictionary—you can create a wide range of optimizers tailored to your specific needs.</p> <p><img src="https://medium.com/_/stat?event=post.clientViewed&amp;referrerSource=full_rss&amp;postId=58fbe987cfae" width="1" height="1" alt=""/></p>]]></content><author><name></name></author></entry><entry><title type="html">The Most Fundamental Unsolved Problem in All of Science and Philosophy</title><link href="https://yehya-farhat.github.io/blog/2023/the-most-fundamental-unsolved-problem-in-all-of-science-and-philosophy/" rel="alternate" type="text/html" title="The Most Fundamental Unsolved Problem in All of Science and Philosophy"/><published>2023-12-30T16:06:25+00:00</published><updated>2023-12-30T16:06:25+00:00</updated><id>https://yehya-farhat.github.io/blog/2023/the-most-fundamental-unsolved-problem-in-all-of-science-and-philosophy</id><content type="html" xml:base="https://yehya-farhat.github.io/blog/2023/the-most-fundamental-unsolved-problem-in-all-of-science-and-philosophy/"><![CDATA[<figure><img alt="" src="https://cdn-images-1.medium.com/max/512/1*rOwDG6egG1lMSgHZRjLx_A.png"/></figure> <h4>What is the biggest unsolved problem?</h4> <p>If you Google this question, you might find a Wikipedia page titled ‘List of unsolved problems’. There, you will find many sublists, each containing a number of open problems within its respective field. You might also ask ChatGPT, read other blogs detailing some problems, or turn to the 7 open problems (now 6) posed by the Clay Mathematics Institute, where the prize for solving any one of them is a million dollars each. But underlying all of this is a huge philosophical question, a question put forward by David Hume in 1739, known as the ‘problem of induction’. Hume makes a very interesting observation that undermines the foundations of rational thinking and our beloved scientific method. A question that has eluded scholars for around 300 years, and one that we still do not have a good answer for:</p> <blockquote>Can we justify the assumption that the future will resemble the past?</blockquote> <h4><strong>Will objects always fall to the ground when dropped?</strong></h4> <p>Do you believe an object will fall to the ground when dropped? Of course, it would; what sort of question is this? We’ve all experienced gravity. True, and I’m sure if you drop something right now, it will fall to the ground; I have no doubt about that. But why? For Hume, the answer was not so obvious, and rightfully so. Think about it: why do you believe that dropping an object will lead to it falling? Well, because of gravity, of course. Fine, but why do you believe that the force of gravity will act on the object when dropped? Well, because every time I’ve dropped something, it has always been pulled down by gravity, and thus I can establish it as a law of nature. The whole basis of you believing that gravity will act on an object once released is based on past observation(s). But this was the problem Hume pointed out. All your inferences rely, directly or indirectly, on a rationally unfounded premise that the future will resemble the past. The basis of the inference that an object will fall to the ground when released is only justifiable if you assume the future ‘must’ resemble the past, but how is the assumption itself justified? Stating that the future has always resembled the past and thus will again is a logical fallacy, a circular argument. You are assuming what you are trying to prove. Therefore, the belief that an object will fall to the ground once dropped, simply because it has been observed in the past, is rationally unjustified. We can extend this argument to most daily experiences or observations in life. Why do you think you will feel heat when you place your hand next to a fire? The same argument applies: I’ve always felt heat when I placed my hand next to a fire, and I expect the same reaction. Perhaps you are beginning to gain more intuition about the problem. Every action we take, from turning on a light and cooking food to planting seeds or using technology, is predicated on the assumption, ingrained through past experiences, that future outcomes will mirror those we’ve encountered before, and for the most part, they do! Yet, we have no satisfactory answer as to why this is or should be the case, other than practicality.</p> <h3><strong>Hume turned Science into a belief system</strong></h3> <p>We would like to think that our scientific method is a systematic and evidence-based approach to understanding the natural world, in which truth is established based on impeccable rationality and verifiable experimentation. But it’s very hard for us to really make such a claim. The seriousness of the question Hume put forward cannot be understated, and it’s important to appreciate the fundamental implications of Hume’s argument. Let’s revisit the gravity example. Assume that tomorrow, while leaving for work, you accidentally drop your keys on the way out, but instead of falling to the ground, they fly straight up in the air for a few centimeters before falling back down. Such an anomaly would require us to accommodate this behavior by explaining it away with some other event that can maintain the consistency of the idea that gravity is a force that is always pointing down and <em>causes </em>things to fall. Astronomers then come out and claim that every 3 million years, an asteroid so big with a massive electromagnetic field passes by Earth and it is close enough to cause some disturbance (I know this is not possible, but bear with me here). But what if something even more ridiculous happens? The sun suddenly starts rising in the west. Well, now we have a much bigger problem. This is not an anomaly anymore. The sun has been rising in the west for a week now! ‘Impossible’, astronomers say. They pull up all their mathematical theories and prove to you why it should always rise in the east based on the rotation of Earth relative to the sun and our galaxy. But the fundamental problem is that our theory is based on two things: historical data such as, the rotations and the speed of our planet, and the galaxy, and the ASSUMPTION that the behavior of our planet and the galaxy will remain relatively stable through the rest of time. With the new behavior of the sun, we suddenly need to reformat our theory and understanding of the cosmos. We will need to then develop a new theory to accommodate and explain this new behavior. Whatever we thought was ‘true’ was based on an underlying assumption that had no proper right to be there besides the fact that we literally cannot do science without it. We sit there, develop all these theories of cosmology but fail to take into account the possibility of the dynamic behaviors of the universe. We humans are only a drop in the vast life of the universe; the cosmological constant might not be so constant. Maybe it changes every 15 billion years in very unpredictable ways. What happens then to all our cosmology theories? We have developed partial differential equations for the rate of change of cancers, but what if tomorrow suddenly the rate we quantified shifts drastically?</p> <p>These might all seem like ridiculous statements. However, the truth is, without the assumption that the past will resemble the future, so too are any statements about the future. Before we can assert what ought to be, it would be prudent to first understand and articulate why it should be. Bertrand Russell put this best in his book ‘The Problems of Philosophy’.</p> <blockquote>“The man who has fed the chicken every day throughout its life at last wrings its neck instead, showing that more refined views as to the uniformity of nature would have been useful to the chicken.”</blockquote> <p><img src="https://medium.com/_/stat?event=post.clientViewed&amp;referrerSource=full_rss&amp;postId=4f3fca8949df" width="1" height="1" alt=""/></p>]]></content><author><name></name></author></entry><entry><title type="html">What Are the Machines Learning?</title><link href="https://yehya-farhat.github.io/blog/2023/MLmodels/" rel="alternate" type="text/html" title="What Are the Machines Learning?"/><published>2023-09-18T10:02:00+00:00</published><updated>2023-09-18T10:02:00+00:00</updated><id>https://yehya-farhat.github.io/blog/2023/MLmodels</id><content type="html" xml:base="https://yehya-farhat.github.io/blog/2023/MLmodels/"><![CDATA[<style>.small-img{max-width:50%;height:auto;display:block;margin:0 auto}.image-container{height:380px;overflow:hidden}.image-container img{width:100%;height:100%;object-fit:cover}</style> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-1"> <div class="image-container"> <figure> <picture> <img src="/assets/img/waml_img2.jpg" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="col-sm mt-3 mt-md-1"> <div class="image-container"> <figure> <picture> <img src="/assets/img/messyequations.jpg" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> </div> <p><br/></p> <p> With the recent surge of AI models, from ChatGPT to Google's latest search-embedded generative AI, this technology is here to stay. Looking ahead, AI systems will become industry staples, a standard tool used by every company, home, and individual, akin to the ubiquity of computers today. AI proficiency will probably be the next set of skills, similar to Microsoft Office skills, that you see required in everyday job postings. With the speed at which this technology is advancing, it's essential to take a step back and look at what these models actually are and why they're so useful! </p> <h2 id="the-domain-of-ai">The domain of AI</h2> <p> Before delving into the world of AI, it's crucial to establish clarity in our terminology. The terms Machine Learning (ML), Artificial Intelligence (AI), and Deep Learning are often used interchangeably, leading to confusion. In essence, AI serves as an umbrella term, akin to "Biology," which encompasses a wide range of subdomains and techniques. AI is frequently employed as a buzzword to secure funding, but it represents a broad and multifaceted field. Within the realm of AI, Machine Learning stands out as its crown jewel. Machine Learning itself comprises a diverse array of techniques, each with its unique characteristics and applications. One of the most renowned and potent branches of Machine Learning is Deep Learning, also known as neural networks. Deep Learning techniques have gained widespread recognition for their ability to handle complex tasks and are at the forefront of many AI breakthroughs. <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <img src="/assets/img/AIdomain.png" class="img-fluid rounded z-depth-0 small-img" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> Here's a helpful image to visualize this hierarchy: Machine Learning encompasses a multitude of techniques, including Decision Trees, Linear Regression, Support Vector Machines (SVM), and Deep Learning (just to name a few). Deep Learning is a field of its own and stands out as one of the most successful ML algorithms. Within Deep Learning, you'll find a wide range of approaches, from Convolutional Neural Networks (used in imaging) to Transformers (applied to language tasks). However, everything I just mentioned falls under the same umbrella term: ML. But how do ML models work, and what do these algorithms do? </p> <p><br/></p> <h2 id="what-does-linear-regression-and-deep-neural-networks-have-in-common">What does Linear regression and Deep Neural Networks have in common?</h2> <p>Spoiler: Linear Regression and Deep Neural Networks are both machine learning algorithms. But what does something you might have studied in high school have to do with the architecture behind ChatGPT? Essentially, these two algorithms are trying to accomplish the same task, albeit in very different ways.</p> <p>The goal behind both these techniques, as well as every other ML technique you will come across, is to estimate or ‘Learn’ a mapping of a given dataset. In other words they aim to find the function \(f(x)\) that describes your data. More formally, given the input data \(X\) and the output data \(Y\) find me an \(f(X)\) that best describes that mapping. The more data points you provide, the better your estimate.</p> <p>Thats it! That’s literally all these models are doing. Well, okay, there’s a lot more that goes into it, but at a high level, that’s the core concept. At its essence, machine learning is a statistical technique. Give me a bunch of points and the algorithm will try and find you the best function that describes/maps those data points.</p> <p>I won’t be delving into the workings of linear regression or neural networks in this post. Those topics are extensive and deserve their own dedicated discussions, and you can easily find countless resources about them online. Instead, we’ll focus on what they accomplish without getting into all the technical intricacies of ‘how’ they achieve it. By the end of this post, I aim to demonstrate why these techniques are highly valuable and prevalent across various domains.</p> <p>Now, let’s start with a quick refresher on what mathematical functions are.</p> <h4>Functions</h4> <p>Here’s the definition of a function i shamelessly stole from wikipedia. <br/> In mathematics, a function from a set \(X\) to a set \(Y\) assigns to each element of \(X\) one element of \(Y\). The set of \(X\) is called the domain of the function and the set \(Y\) is called the codomain or range of the function.</p> <p>So in layman terms, you can think of a function as a sort of machine. You give it a certain input, \(x\) it performs certain operations on this input, and then it produces an output \(y\). It’s also worth noting that almost all of scientific theories are built upon functions. From quantum mechanical descriptions to tumor growth rates.</p> <p>Here’s an example of a simple function: \(f(x) = x^2\) <br/> In this function, whatever number you input, the function will square that number, resulting in the corresponding answer. \(2 \rightarrow f(2) \rightarrow 4\) <br/></p> <p>Functions are usually derived through empirical observations or developed mathematically based on a set of rules. For instance, Einstein derived his famous equation, \(E=mc^2\) by establishing the speed of light as a constant. And voilà, you now have a function that describes the relationship between mass and energy. When you input a value for the mass, you can obtain the corresponding energy value, or if you reverse the process and input energy, you’ll get the mass value.</p> <p>But as mentioned earlier, equations can also be derived solely based on empirical observation, coupled with a bit of intuition, of course. For instance, when you aim to model population growth, you examine your data and formulate an equation that best fits the population data you’ve collected. This approach can be more challenging because it often involves a considerable amount of trial and error, and it can lead to biased estimations based on the sample data you have on hand (foreshadowing much?) That is not to say that deriving equations is easier. There’s a lot of trial and error there too and those theories are also biased in their own way. We don’t need to look too far for an example; consider Newton’s work on gravity. His equations could not describe pluto’s orbit or the bending of light due to gravitational affects. Newton simply didn’t have access to the data needed to test his theory and had to work with the observations of an apple and the moon.</p> <h4>Hunting for Hidden Formulas</h4> <p>Here, let’s try to derive an equation from empirical observation. I will give you a bunch of data points, and you find the function (pattern)!</p> <table> <thead> <tr> <th>\(X\)</th> <th style="text-align: center">\(Y\)</th> </tr> </thead> <tbody> <tr> <td>1</td> <td style="text-align: center">1</td> </tr> <tr> <td>2</td> <td style="text-align: center">8</td> </tr> <tr> <td>3</td> <td style="text-align: center">27</td> </tr> <tr> <td>4</td> <td style="text-align: center">64</td> </tr> <tr> <td>\(\vdots\)</td> <td style="text-align: center">\(\vdots\)</td> </tr> </tbody> </table> <p><br/> You probably guessed it, the function is</p> <details> <summary>Click to reveal answer</summary> $$f(x) = x^3$$ you simply multiply the number by itself three times. </details> <p><br/> <br/></p> <p>Good, let’s complicate our example. This time, our function takes two positive inputs. To simplify matters, we’ll set the second input (\(Z\)) as a constant value. This function may remind you of something you encountered in high school physics. Your task is to work with this function with the constant Z and determine the relationship between the two variables that best describes it.</p> <table> <thead> <tr> <th>\(X;Z\)</th> <th style="text-align: center">\(Y\)</th> </tr> </thead> <tbody> <tr> <td>1 ; 9.8</td> <td style="text-align: center">9.8</td> </tr> <tr> <td>10 ; 9.8</td> <td style="text-align: center">98</td> </tr> <tr> <td>4 ; 9.8</td> <td style="text-align: center">39.2</td> </tr> <tr> <td>7 ; 9.8</td> <td style="text-align: center">68.6</td> </tr> <tr> <td>\(\vdots\)</td> <td style="text-align: center">\(\vdots\)</td> </tr> </tbody> </table> <p><br/> This was also probably a straight giveaway from the first input.</p> <details> <summary>Click to reveal answer</summary> You multiply the two numbers together. looks a bit like Newtons second law, no? $$F=ma$$ Just set "z" to 9.8 (acceleration of Earth's gravity) and let the input x represent the mass of the object. </details> <p><br/> <br/></p> <p>I’ve been providing relatively straightforward examples so far, with inputs given in a specific order and corner cases to aid in deduction. Now, let’s consider a different scenario. I’ll present a function that takes positive inputs, but this time, the data isn’t sorted, and I won’t offer any obvious hints. Your challenge is to determine the function that most accurately represents this data.</p> <table> <thead> <tr> <th>\(X;Z\)</th> <th style="text-align: center">\(Y\)</th> </tr> </thead> <tbody> <tr> <td>7 ; 2</td> <td style="text-align: center">7.82</td> </tr> <tr> <td>3 ; 9</td> <td style="text-align: center">9.42</td> </tr> <tr> <td>5 ; 6</td> <td style="text-align: center">7.81</td> </tr> <tr> <td>4 ; 8</td> <td style="text-align: center">8.94</td> </tr> <tr> <td>2 ; 2</td> <td style="text-align: center">2.83</td> </tr> <tr> <td>10 ; 15</td> <td style="text-align: center">18.03</td> </tr> <tr> <td>8 ; 9</td> <td style="text-align: center">12.04</td> </tr> <tr> <td>3 ; 7</td> <td style="text-align: center">7.62</td> </tr> <tr> <td>\(\vdots\)</td> <td style="text-align: center">\(\vdots\)</td> </tr> </tbody> </table> <p><br/> If you’ve discovered this, I salute you; if you haven’t, I don’t blame you. Finding this function might have taken some time. The function in question is</p> <details> <summary>Click to reveal answer</summary> none other than the famous Pythagorean theorem $$x^2 + z^2 = y^2$$ </details> <p><br/> <br/></p> <p>The exercises I’ve presented so far were merely illustrative examples. None of these functions necessarily had to be derived in this particular manner. However, consider what happens when we encounter a scenario where we genuinely need to do this. What if we possess a dataset of a specific function but lack any insight into the actual function itself? You can appreciate how challenging this can become for us, ordinary humans.<br/></p> <p>Let’s explore a real-life situation where this challenge is indeed applicable: image classification! Here’s the scenario: can you provide me with a function that takes as input an image containing either a cat or a dog and outputs either 0, to signify a cat, or 1, to denote a dog? An image is essentially represented mathematically as an array (flattened matrix) that encodes pixel values. However, you don’t need to focus on the technical details of this representation. The critical point here is that our function takes an input matrix, denoted as A, which describes the pixel content of the image.</p> <p>For clarity, let’s consider \(A_1\) as the matrix representing a dog image and \(A_0\) as the matrix representing a cat image. Your task is to provide the function that best captures the relationship between these input matrices to distinguish between cat and dog images accurately.</p> <table> <thead> <tr> <th>\(X\)</th> <th style="text-align: center">\(Y\)</th> </tr> </thead> <tbody> <tr> <td>\(A_0\)</td> <td style="text-align: center">0</td> </tr> <tr> <td>\(A_1\)</td> <td style="text-align: center">1</td> </tr> </tbody> </table> <p><br/> This is already getting into the realm of impossibility and its even more challenging when we consider that we’ve only had to deal with two inputs and two outputs. Now, picture the complexity of classifying ten different animals, each captured from slightly different angles. <br/></p> <p>This is where the value of ML models truly shines. Each ML model offers a unique representation of the underlying function. For example, deep neural networks discover weight matrices, while K-Means Clustering identifies clusters in a hyperspace. Regardless of the specific technique, these models perform numerous calculations that would likely have taken you years to do yourself to manually derive arbitrary set of representations to effectively describe the data.</p> <p>In today’s data-driven world, the significance of machine learning cannot be overstated. As we grapple with increasingly large and intricate datasets, machine learning becomes our compass, guiding us through uncharted territories of information. It accelerates discovery, transforming mountains of raw data into valuable insights. Whether it’s advancing medical research by identifying new treatment pathways, enhancing customer experiences through personalized recommendations, or predicting market trends, machine learning has become an indispensable tool across diverse industries.</p>]]></content><author><name></name></author><category term="MachineLearning"/><category term="MachineLearning"/><summary type="html"><![CDATA[With the recent surge of AI models, from ChatGPT to Google's latest search-embedded generative AI, this technology is here to stay. Looking ahead, AI systems will become industry staples, a standard tool used by every company, home, and individual, akin to the ubiquity of computers today. AI proficiency will probably be the next set of skills, similar to Microsoft Office skills, that you see required in everyday job postings. With the speed at which this technology is advancing, it's essential to take a step back and look at what these models actually are and why they're so useful! The domain of AI Before delving into the world of AI, it's crucial to establish clarity in our terminology. The terms Machine Learning (ML), Artificial Intelligence (AI), and Deep Learning are often used interchangeably, leading to confusion. In essence, AI serves as an umbrella term, akin to "Biology," which encompasses a wide range of subdomains and techniques. AI is frequently employed as a buzzword to secure funding, but it represents a broad and multifaceted field. Within the realm of AI, Machine Learning stands out as its crown jewel. Machine Learning itself comprises a diverse array of techniques, each with its unique characteristics and applications. One of the most renowned and potent branches of Machine Learning is Deep Learning, also known as neural networks. Deep Learning techniques have gained widespread recognition for their ability to handle complex tasks and are at the forefront of many AI breakthroughs. Here's a helpful image to visualize this hierarchy: Machine Learning encompasses a multitude of techniques, including Decision Trees, Linear Regression, Support Vector Machines (SVM), and Deep Learning (just to name a few). Deep Learning is a field of its own and stands out as one of the most successful ML algorithms. Within Deep Learning, you'll find a wide range of approaches, from Convolutional Neural Networks (used in imaging) to Transformers (applied to language tasks). However, everything I just mentioned falls under the same umbrella term: ML. But how do ML models work, and what do these algorithms do? What does Linear regression and Deep Neural Networks have in common? Spoiler: Linear Regression and Deep Neural Networks are both machine learning algorithms. But what does something you might have studied in high school have to do with the architecture behind ChatGPT? Essentially, these two algorithms are trying to accomplish the same task, albeit in very different ways. The goal behind both these techniques, as well as every other ML technique you will come across, is to estimate or ‘Learn’ a mapping of a given dataset. In other words they aim to find the function \(f(x)\) that describes your data. More formally, given the input data \(X\) and the output data \(Y\) find me an \(f(X)\) that best describes that mapping. The more data points you provide, the better your estimate. Thats it! That’s literally all these models are doing. Well, okay, there’s a lot more that goes into it, but at a high level, that’s the core concept. At its essence, machine learning is a statistical technique. Give me a bunch of points and the algorithm will try and find you the best function that describes/maps those data points. I won’t be delving into the workings of linear regression or neural networks in this post. Those topics are extensive and deserve their own dedicated discussions, and you can easily find countless resources about them online. Instead, we’ll focus on what they accomplish without getting into all the technical intricacies of ‘how’ they achieve it. By the end of this post, I aim to demonstrate why these techniques are highly valuable and prevalent across various domains. Now, let’s start with a quick refresher on what mathematical functions are. Functions Here’s the definition of a function i shamelessly stole from wikipedia. In mathematics, a function from a set \(X\) to a set \(Y\) assigns to each element of \(X\) one element of \(Y\). The set of \(X\) is called the domain of the function and the set \(Y\) is called the codomain or range of the function. So in layman terms, you can think of a function as a sort of machine. You give it a certain input, \(x\) it performs certain operations on this input, and then it produces an output \(y\). It’s also worth noting that almost all of scientific theories are built upon functions. From quantum mechanical descriptions to tumor growth rates. Here’s an example of a simple function: \(f(x) = x^2\) In this function, whatever number you input, the function will square that number, resulting in the corresponding answer. \(2 \rightarrow f(2) \rightarrow 4\) Functions are usually derived through empirical observations or developed mathematically based on a set of rules. For instance, Einstein derived his famous equation, \(E=mc^2\) by establishing the speed of light as a constant. And voilà, you now have a function that describes the relationship between mass and energy. When you input a value for the mass, you can obtain the corresponding energy value, or if you reverse the process and input energy, you’ll get the mass value. But as mentioned earlier, equations can also be derived solely based on empirical observation, coupled with a bit of intuition, of course. For instance, when you aim to model population growth, you examine your data and formulate an equation that best fits the population data you’ve collected. This approach can be more challenging because it often involves a considerable amount of trial and error, and it can lead to biased estimations based on the sample data you have on hand (foreshadowing much?) That is not to say that deriving equations is easier. There’s a lot of trial and error there too and those theories are also biased in their own way. We don’t need to look too far for an example; consider Newton’s work on gravity. His equations could not describe pluto’s orbit or the bending of light due to gravitational affects. Newton simply didn’t have access to the data needed to test his theory and had to work with the observations of an apple and the moon. Hunting for Hidden Formulas Here, let’s try to derive an equation from empirical observation. I will give you a bunch of data points, and you find the function (pattern)! \(X\) \(Y\) 1 1 2 8 3 27 4 64 \(\vdots\) \(\vdots\) You probably guessed it, the function is Click to reveal answer $$f(x) = x^3$$ you simply multiply the number by itself three times. Good, let’s complicate our example. This time, our function takes two positive inputs. To simplify matters, we’ll set the second input (\(Z\)) as a constant value. This function may remind you of something you encountered in high school physics. Your task is to work with this function with the constant Z and determine the relationship between the two variables that best describes it. \(X;Z\) \(Y\) 1 ; 9.8 9.8 10 ; 9.8 98 4 ; 9.8 39.2 7 ; 9.8 68.6 \(\vdots\) \(\vdots\) This was also probably a straight giveaway from the first input. Click to reveal answer You multiply the two numbers together. looks a bit like Newtons second law, no? $$F=ma$$ Just set "z" to 9.8 (acceleration of Earth's gravity) and let the input x represent the mass of the object. I’ve been providing relatively straightforward examples so far, with inputs given in a specific order and corner cases to aid in deduction. Now, let’s consider a different scenario. I’ll present a function that takes positive inputs, but this time, the data isn’t sorted, and I won’t offer any obvious hints. Your challenge is to determine the function that most accurately represents this data. \(X;Z\) \(Y\) 7 ; 2 7.82 3 ; 9 9.42 5 ; 6 7.81 4 ; 8 8.94 2 ; 2 2.83 10 ; 15 18.03 8 ; 9 12.04 3 ; 7 7.62 \(\vdots\) \(\vdots\) If you’ve discovered this, I salute you; if you haven’t, I don’t blame you. Finding this function might have taken some time. The function in question is Click to reveal answer none other than the famous Pythagorean theorem $$x^2 + z^2 = y^2$$ The exercises I’ve presented so far were merely illustrative examples. None of these functions necessarily had to be derived in this particular manner. However, consider what happens when we encounter a scenario where we genuinely need to do this. What if we possess a dataset of a specific function but lack any insight into the actual function itself? You can appreciate how challenging this can become for us, ordinary humans. Let’s explore a real-life situation where this challenge is indeed applicable: image classification! Here’s the scenario: can you provide me with a function that takes as input an image containing either a cat or a dog and outputs either 0, to signify a cat, or 1, to denote a dog? An image is essentially represented mathematically as an array (flattened matrix) that encodes pixel values. However, you don’t need to focus on the technical details of this representation. The critical point here is that our function takes an input matrix, denoted as A, which describes the pixel content of the image. For clarity, let’s consider \(A_1\) as the matrix representing a dog image and \(A_0\) as the matrix representing a cat image. Your task is to provide the function that best captures the relationship between these input matrices to distinguish between cat and dog images accurately. \(X\) \(Y\) \(A_0\) 0 \(A_1\) 1 This is already getting into the realm of impossibility and its even more challenging when we consider that we’ve only had to deal with two inputs and two outputs. Now, picture the complexity of classifying ten different animals, each captured from slightly different angles. This is where the value of ML models truly shines. Each ML model offers a unique representation of the underlying function. For example, deep neural networks discover weight matrices, while K-Means Clustering identifies clusters in a hyperspace. Regardless of the specific technique, these models perform numerous calculations that would likely have taken you years to do yourself to manually derive arbitrary set of representations to effectively describe the data. In today’s data-driven world, the significance of machine learning cannot be overstated. As we grapple with increasingly large and intricate datasets, machine learning becomes our compass, guiding us through uncharted territories of information. It accelerates discovery, transforming mountains of raw data into valuable insights. Whether it’s advancing medical research by identifying new treatment pathways, enhancing customer experiences through personalized recommendations, or predicting market trends, machine learning has become an indispensable tool across diverse industries.]]></summary></entry></feed>